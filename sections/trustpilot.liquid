<section id="section-{{ section.id }}" data-section-id="{{ section.id }}" data-section-type="trustpilot">
  {% render 'trustpilot-widget' %}
</section>

{% schema %}
  {
    "name": "Section name",
    "settings": []
  }
{% endschema %}
